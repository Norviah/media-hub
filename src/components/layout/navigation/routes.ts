import { SearchIcon } from 'lucide-react';
import type { RouteItem } from '@/types/Route';

export const nav: RouteItem[] = [
  {
    title: 'Search',
    path: '/search',
    icon: SearchIcon,
  },
];
